# Context Budget

- tier: `governed`
- limit: `1300` estimated tokens
- estimated initial load: `744`
- Skill body: `480`
- interface: `264`
- deferred resources: `10215`
- warnings: `0`
- unused resource directories: `0`
- quality density: `188.2`

初始加载只包含精简 `SKILL.md` 与 `agents/interface.yaml`。策略、确定性逻辑、评测和报告均按需延迟读取。本结果是 `resource_boundary_check.py` 的本地启发式估算，不是模型 token 账单。
